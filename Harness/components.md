# Components

## When To Load

- Use this compatibility entrypoint when an older instruction points to `Harness/components.md`.
- Use for reusable SwiftUI helper, wrapper, modifier, preview-helper, or shared UI utility work.

## Guidance

Reusable UI component and helper guidance is owned by [Harness/ui_components.md](ui_components.md).

Do not duplicate component rules here. Update the owner file instead.
